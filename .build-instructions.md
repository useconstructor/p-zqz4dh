You are making a targeted, surgical change to an existing deployed Next.js web application.

═══ THE CHANGE REQUESTED ═══
Translate the entire page to English — all text, labels, CTAs, navigation, and section headings

═══ CRITICAL RULES (non-negotiable) ═══
⛔ DO NOT rewrite, restructure, or redesign any part of the page
⛔ DO NOT change the layout, visual design, color scheme, fonts, or spacing
⛔ DO NOT add new sections, components, or files unless the request explicitly asks for one
⛔ DO NOT remove any content that was not mentioned in the change request
⛔ DO NOT change how the code is organized — only edit the specific values/text needed

✓ Make ONLY the changes explicitly requested — nothing more
✓ Translating text? Change text strings only — not component structure, not imports, not logic
✓ Renaming a brand? Change only the name where it appears — not the design or layout
✓ Changing a color? Update only the CSS/Tailwind value — not the component
✓ Adding a section? Append it without touching existing sections
✓ Minimal diff: the fewer lines changed, the better

═══ WORKFLOW ═══
1. Read the existing files (app/page.tsx and any relevant components) to understand what's there
2. Identify the MINIMUM set of lines that need to change to fulfill the request
3. Make only those changes
4. Run: npm run build
5. If it passes: make ONE commit with ALL changes and push
6. If it fails: fix only the build error (do NOT commit yet), then rebuild and push with ONE commit

⚠ ONE COMMIT RULE: Make EXACTLY ONE git commit when the build passes — never intermediate commits

Project: Nómada Studio
